#include <stdio.h>
#include <math.h>
#include "NumClass.h"
void main()
{
    // printf("1 for prime and 0 for not prime\n%d", isPrime(7));
    printf("1 is armstrong and 0 is not: %d\n", isArmstrong(9474));
}